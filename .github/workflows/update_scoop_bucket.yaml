name: Update Scoop manifest (moved)

on: workflow_dispatch

permissions: {}

jobs:
  moved:
    name: Dispatch moved to CI - Windows
    runs-on: ubuntu-latest
    steps:
      - name: Explain workflow location
        run: echo "Scoop dispatch now runs as the update-scoop job in CI - Windows after a successful master build."
