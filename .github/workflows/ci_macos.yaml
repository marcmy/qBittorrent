name: CI - macOS (disabled)

on: workflow_dispatch

permissions: {}

jobs:
  disabled:
    name: macOS CI disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain status
        run: echo "Automatic macOS builds are disabled for this Windows-focused fork."
